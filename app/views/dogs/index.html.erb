<div class="main-dogs-container">
  <div class="dogs-container">
  <h6 class="header">There are <%= @dogs.all.length %> Dogs in Your Location</h6>
    <div class="container">
      <div class="cards">
      <% @dogs.each do |dog| %>
        <div class="card" style="width: 12rem;">
          <%= link_to dog_path(dog.id) do %>
          <%= image_tag "choco_lab.jpg", class:"card-img-top" %>
          <% end %>
          <div class="index-card-body">
            <h3 class="index-card-title"><%= dog.name %></h3>
            <p><%= dog.description %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
    <%# if policy(Dog).create? %>
      <%#= link_to "New Dog", new_dog_path %>
    <%# end %>
  </div>
  <% if !Dog.all.nil? %>
  <div
    id="map"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  ></div>
  <% end %>
</div>

